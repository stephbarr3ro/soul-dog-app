import React, { useState, useEffect } from 'react';
import { useSearchParams, useNavigate } from 'react-router-dom';
import { ChevronLeft, ChevronRight, ShoppingBag } from 'lucide-react';
import { motion, AnimatePresence } from 'motion/react';
import { useCustomizationStore } from '@/src/store/useCustomizationStore';
import { Step0_Edition } from '@/src/components/customization/Step0_Edition';
import { Step1_Children } from '@/src/components/customization/Step1_Children';
import { Step2_Dogs } from '@/src/components/customization/Step2_Dogs';
import { Step3_StoryTitle } from '@/src/components/customization/Step3_StoryTitle';
import { Step4_FinalTouches } from '@/src/components/customization/Step4_FinalTouches';
import { Step5_Review } from '@/src/components/customization/Step5_Review';
import { LivePreview } from '@/src/components/customization/LivePreview';
import { cn } from '@/src/lib/utils';
import { createCheckoutAndRedirect, getProductByHandle } from '@/src/lib/shopify';
const STEPS=[{id:'edition',label:'Edition'},{id:'children',label:'Children'},{id:'dogs',label:'Dogs'},{id:'story',label:'Story'},{id:'final',label:'Final'},{id:'review',label:'Review'}];
const StepIndicator=({currentStep}:{currentStep:number})=>(<div className="flex items-center gap-3 overflow-x-auto pb-2 scrollbar-hide">{STEPS.map((step,index)=>(<div key={step.id} className="flex flex-col gap-1.5 min-w-[52px]"><div className={cn("h-0.5 rounded-full transition-all duration-700",currentStep===index?"bg-navy w-full":currentStep>index?"bg-gold w-full":"bg-gray-100 w-5")}/><span className={cn("text-[8px] font-bold uppercase tracking-[0.25em] transition-colors",currentStep===index?"text-navy":"text-gray-300")}>{step.label}</span></div>))}</div>);
export const Customize=()=>{
const [searchParams]=useSearchParams();
const navigate=useNavigate();
const {edition,setEdition,children,dogs,storyTitle,coverColor,dedication,giftWrapping}=useCustomizationStore();
const [currentStep,setCurrentStep]=useState(0);
const [isProcessing,setIsProcessing]=useState(false);
useEffect(()=>{const e=searchParams.get('edition');if(e==='classic'||e==='true-likeness'){setEdition(e);setCurrentStep(1);}},[searchParams,setEdition]);
const handleNext=()=>{if(currentStep<STEPS.length-1){setCurrentStep(currentStep+1);window.scrollTo(0,0);}else{handleAddToCart();}};
const handleBack=()=>{if(currentStep>0){setCurrentStep(currentStep-1);window.scrollTo(0,0);}else{navigate('/');}};
const handleAddToCart=async()=>{setIsProcessing(true);try{const h=edition==='classic'?'classic-edition':'true-likeness-edition';const p=await getProductByHandle(h);if(!p)throw new Error('not found');const cp:Record<string,string>={'Edition':edition==='classic'?'Classic Edition':'True Likeness Edition','Story Title':storyTitle,'Cover Color':coverColor,'Dedication':dedication,'Gift Wrapping':giftWrapping?'Yes':'No'};children.forEach((c,i)=>{cp[`Child ${i+1} Name`]=c.name;cp[`Child ${i+1} Gender`]=c.gender;cp[`Child ${i+1} Age`]=c.ageRange;});dogs.forEach((d,i)=>{cp[`Dog ${i+1} Name`]=d.name;cp[`Dog ${i+1} Breed`]=d.breed;cp[`Dog ${i+1} Fur`]=d.furColor;cp[`Dog ${i+1} Collar`]=d.collarColor;});await createCheckoutAndRedirect([{variantId:p.variants[0].id,quantity:1,customAttributes:Object.entries(cp).map(([k,v])=>({key:k,value:v}))}]);}catch(e){console.error(e);alert('Could not connect to Shopify.');}finally{setIsProcessing(false);}};
const renderStep=()=>{switch(currentStep){case 0:return<Step0_Edition/>;case 1:return<Step1_Children/>;case 2:return<Step2_Dogs/>;case 3:return<Step3_StoryTitle/>;case 4:return<Step4_FinalTouches/>;case 5:return<Step5_Review/>;default:return null;}};
const FixedNavBar=()=>(<div className={`fixed bottom-0 left-0 w-full ${currentStep>=4?'md:w-1/2':''} bg-white/90 backdrop-blur-2xl border-t border-gray-100 p-5 flex ${currentStep>=4?'gap-4':'justify-center'} z-40`}><div className={`flex gap-4 ${currentStep>=4?'w-full':'w-full max-w-2xl'}`}><button onClick={handleBack} className="px-6 py-4 rounded-full border border-gray-200 font-bold text-navy/40 hover:border-navy hover:text-navy transition-all flex items-center gap-2 text-xs uppercase tracking-widest"><ChevronLeft className="w-4 h-4"/>Back</button><button onClick={handleNext} disabled={isProcessing} className={cn("flex-1 px-6 py-4 rounded-full bg-navy text-cream font-bold hover:bg-gold transition-all flex items-center justify-center gap-3 shadow-2xl text-xs uppercase tracking-widest",isProcessing&&"opacity-70 cursor-not-allowed")}>{isProcessing?<>Processing...<motion.div animate={{rotate:360}} transition={{repeat:Infinity,duration:1}} className="w-4 h-4 border-2 border-cream border-t-transparent rounded-full"/></>:currentStep===STEPS.length-1?<>Complete &amp; Checkout<ShoppingBag className="w-4 h-4"/></>:<>Continue<ChevronRight className="w-4 h-4"/></>}</button></div></div>);
if(currentStep===0){return(<div className="min-h-screen bg-white flex flex-col items-center"><div className="w-full max-w-2xl px-6 md:px-10 pt-8 pb-32"><StepIndicator currentStep={0}/><div className="mt-8"><Step0_Edition/></div></div><div className="fixed bottom-0 left-0 w-full bg-white/90 backdrop-blur-2xl border-t border-gray-100 p-5 flex justify-center z-40"><div className="flex gap-4 w-full max-w-2xl"><button onClick={handleBack} className="px-6 py-4 rounded-full border border-gray-200 font-bold text-navy/40 hover:border-navy hover:text-navy transition-all flex items-center gap-2 text-xs uppercase tracking-widest"><ChevronLeft className="w-4 h-4"/>Back</button><button onClick={handleNext} className="flex-1 px-6 py-4 rounded-full bg-navy text-cream font-bold hover:bg-gold transition-all flex items-center justify-center gap-3 text-xs uppercase tracking-widest">Continue<ChevronRight className="w-4 h-4"/></button></div></div></div>);}
return(<div className="min-h-screen bg-white flex flex-col md:flex-row"><div className={`w-full ${currentStep >= 4 ? "md:w-1/2" : "md:w-full"} overflow-y-auto md:max-h-screen scrollbar-hide`}><div className="md:hidden"><div className={`${currentStep >= 4 ? "bg-navy" : "hidden"} overflow-hidden`} style={{minHeight:320}}><LivePreview className="w-full py-8 px-6" step={currentStep} compact/></div><div className="px-6 pt-5 pb-2"><StepIndicator currentStep={currentStep}/></div><div className="px-6 pb-28"><AnimatePresence mode="wait"><motion.div key={currentStep} initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-20}} transition={{duration:0.4}}>{renderStep()}</motion.div></AnimatePresence></div></div><div className="hidden md:block px-10 lg:px-16 pt-10 pb-28"><div className="mb-10"><StepIndicator currentStep={currentStep}/></div><div className="max-w-xl mx-auto"><AnimatePresence mode="wait"><motion.div key={currentStep} initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-20}} transition={{duration:0.4}}>{renderStep()}</motion.div></AnimatePresence></div></div><FixedNavBar/></div><div className={`${currentStep >= 4 ? "hidden md:flex" : "hidden"} w-1/2 bg-navy items-center justify-center p-12 lg:p-24 relative overflow-hidden sticky top-0 h-screen`}><div className="absolute inset-0 opacity-20"><div className="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-gold/10 to-transparent"/></div><LivePreview className="w-full h-full relative z-10" step={currentStep}/></div></div>);};
